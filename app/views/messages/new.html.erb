<div class="container">
    <%= simple_form_for [@challenge, @message] do |f| %>
        <%= f.input :content, label: "Ask a question", require: true, input_html: { class: 'form-control neumorphic-input' } %>
        <%= f.button :submit, 'Submit', class: 'neumorphic-button' %>
    <% end %>
</div>